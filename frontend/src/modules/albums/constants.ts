/** Only one album exists in this prototype's scope; the id still threads through routes for future albums. */
export const CURRENT_ALBUM_ID = 'eye-of-the-nile'

/** Completion milestones — checkpoints pay their own bonus once, claimed as their own interaction. */
export const MILESTONES = [20, 40, 60, 80, 100]
export const MILESTONE_BONUS = [5, 12, 25, 60, 150]
